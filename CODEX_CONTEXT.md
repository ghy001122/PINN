# Codex Context

## Low-Token First Read

For every non-trivial task read only this file and `docs/research_strategy/active_phase.md` first. Load longer context through `docs/research_strategy/context_loading_policy.md`.

Canonical full handoff: `docs/research_strategy/codex_new_dialog_handoff_d23a576.md`.
Project goal: `PROJECT_GOAL.md`.
Current dashboard: `docs/research_strategy/goal_status.md`.

## Current Paper Line

The defensible main manuscript line is calibration-gated constrained `gamma_sub` recovery under fixed or tightly bounded priors in a one-dimensional synthetic numerical digital-twin benchmark. The contribution is identifiability-guided target-space reduction and a mapped confounding boundary, not optimizer novelty or full hidden-field recovery.

OASIS-PINN is a supplementary/high-risk extension for literature-guided multilayer physics, segmented terminals, active observation design, and strict neural-solver audits. It has not displaced the main line.

## V10 Gate Snapshot

- P0 physical semantics: `qualified_supported` as a reduced synthetic implementation.
- P1 CV multidomain training: `failed_but_informative`; median `E_T=0.37563055753707886`, `E_m=0.06811526417732239`, interface residual `106.15460205078125`, success rate `0.0`.
- P2 repaired noisy inverse: `failed_but_informative`; material-specific electrical/transition blocks show local evidence, but thermal blocks, full-rank protocol selection, and interval coverage fail.
- P3 segmented-electrode y-z forward: `qualified_supported` for forward/current integration and local observability only; uniform-limit error `7.666968074108158e-12`, rank `1` to `3`.
- P4 STL/Fourier/F-SPS: blocked by failed P1; no positive superiority claim.
- Generalization: preflight only; stack, interface, pulse, and cross-material transfer remain weak.

## Current Claim Boundary

Allowed now: synthetic evidence for constrained reduced `gamma_sub` inversion, v10 physical-topology implementation, and segmented-terminal forward/local-observability support, with their stated priors and reduced-model boundaries.

Not allowed now: experimental validation, port-only full-field recovery, terminal-only 2D solved, unconditional device parameters, full STL reproduction, universal F-SPS/Fourier superiority, or full FEM/3D/device-grade reproduction.

## Highest Priority

Repair P1 residual scaling, boundary-face/local-coordinate semantics, and staged optimization before any P4 algorithm comparison. Do not start new experiments during governance-only work.

## Frozen Boundary

Ground Truth v1.1 remains frozen. All current evidence is synthetic unless an explicit provenance-backed external or experimental dataset is added.
