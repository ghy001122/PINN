# Next Actions

## Authoritative Current Queue

- Phase/checkpoint: `Q2_PHASE1G_M1_PROTOCOL_SELECTED_EQUILIBRIUM_MANIFOLD_MVE` /
  `Q2_M1_PROTOCOL_SELECTED_EQUILIBRIUM_MANIFOLD_MVE_V1_GO`.
- Retained numerical assets: the contact-aware conservative M1 operator and
  the versioned Qiu-anchored major-branch implementation; all prescribed-state
  direct/latent neural-forward expansion remains stopped.
- Completed: PR #41 is frozen and squash-merged unchanged; four monotone
  protocols complete with `132/132` valid main points. Two cooling events and
  their half-step checks pass, and all 20 unique sampled spectrum states are
  stable. G0/G1 both meet the stable selected-manifold gates, yielding
  `GO_PROTOCOL_SELECTED_EQUILIBRIUM_MANIFOLD` without neural training.
- Preserve `NO_GO_CC_B_STABLE_PATTERNED_TRANSITION_SPAN` and its 34/34
  positive-unstable records as immutable historical/reviewer-defense evidence.

## Single Next Priority

Under fresh authorization, execute only
`Q2_PROTOCOL_MANIFOLD_BRANCH_AWARE_SURROGATE_MVE_V1`: freeze a future split,
compare analytic and ridge baselines with single-head versus physically gated
heating/cooling latent heads, retain conservative projection, and require an
unknown-protocol set output/refusal contract. Do not use root IDs, train in this
round, or expand to full hysteresis, dynamic RC, inverse, MoE, NbO2, or formal
OOD.
