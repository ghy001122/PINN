# Next Actions

## Authoritative Current Queue

- Phase/checkpoint: `Q2_PHASE1H_PROTOCOL_MANIFOLD_BRANCH_AWARE_SURROGATE_MVE` /
  `Q2_PROTOCOL_MANIFOLD_BRANCH_AWARE_SURROGATE_MVE_V1_NO_GO`.
- Retained numerical assets: the contact-aware conservative M1 operator and
  the versioned Qiu-anchored major-branch implementation; all prescribed-state
  direct/latent neural-forward expansion remains stopped.
- Completed: PR #42 is frozen and squash-merged unchanged. G2/G3 close the
  2x2 physical context matrix with `132/132` new main points, two reproduced
  cooling events, and `16/16` stable required spectra. The train-only rank-2
  POD and one seed of H/S/G training complete, but the gated heads fail the
  headline, branch-separation, and unknown-protocol set-coverage gates;
  disposition `NO_GO_PROTOCOL_MANIFOLD_NEURAL_VALUE`.
- Preserve `NO_GO_CC_B_STABLE_PATTERNED_TRANSITION_SPAN` and its 34/34
  positive-unstable records as immutable historical/reviewer-defense evidence.

## Single Next Priority

Consolidate the direct-PINN, prescribed-state latent, single-valued IMT, and
protocol-manifold surrogate failures into a limitation/negative manuscript
section while retaining the M1 operator, four-context protocol fields, and
fixed ridge/POD artifacts for deterministic solver workflows. Do not launch
formal OOD, another forward-neural rescue, MoE, STL, inverse, dynamic RC,
NbO2, or a threshold/architecture search.
