# Next Actions

## Authoritative Current Queue

- Phase/checkpoint: `Q2_PHASE1C_M1_ROBIN_CONTROL_VOLUME_PINN_RESCUE` /
  `Q2_M1_ROBIN_CONTROL_VOLUME_PINN_RESCUE_V1_VALID_NO_GO`.
- Active fallback: R1-Lite `GeoState-MC-Hybrid-PINN` on
  `state-conditioned quasi-static electrothermal fields`.
- Completed: PR #37 is frozen and merged unchanged; 12/12 M1 teachers pass the
  discrete Robin/CV compatibility contract; B0-R/B1-R/P0-RCV each completed
  1500 Adam steps with the same 5% anchors, but each passed 0/2 complete test
  cases. Disposition is `NO_GO_M1_RCV_PINN_RESCUE`.
- Preserve `NO_GO_CC_B_STABLE_PATTERNED_TRANSITION_SPAN` and its 34/34
  positive-unstable records as immutable historical/reviewer-defense evidence.

## Single Next Priority

Preregister, but do not yet execute, a solver-projected conservative surrogate
that learns a reduced state while the frozen M1 FVM operator supplies fields,
ports, and ledgers. Direct coordinate-PINN rescue, formal OOD, inverse, MoE,
NbO2, dynamic RC, and current-clamp work remain unauthorized.
