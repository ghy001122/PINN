# Next Actions

## Authoritative Current Queue

Active phase: `Q2_PHASE1_2P5D_REFERENCE_SOLVER`.

Checkpoint state: `B_READINESS_V8_NO_GO_VERTICAL_REFERENCE`.

There is no authorized compute task.

> Stop the current K-state route and obtain an explicit user decision on a new
> physical model, a different reduction route, or a manuscript/delivery
> downgrade. Checkpoint B and its 96-case formal campaign remain blocked.

## Manuscript Use

This solver is the numerical truth judge for later R1 fields, ports, phase events, interfaces, RC behavior, and energy ledgers. Without it, no positive new-route PINN claim is eligible.

## Locked Evidence And Disposition

The final bounded v8 screen executed from preregistration SHA
`a32375b74772da8192d390f4233ed0b15e23ae80` and config SHA-256
`e047d7963c646cabdec9796a2f227c159750a76170805a6f02021e6fff24b00b`.
The primary and conditional maximum raw pairs passed every mesh, passivity,
identity, and finite foundation check. On the separately gated formal-window
pullback, however, bare/contact depth-frequency RMSEs were approximately
`0.41181/0.41178` for 51.2/102.4 micrometres and
`0.41180/0.41179` for 102.4/204.8 micrometres, all above `0.05`.

Disposition: `NO_GO_VERTICAL_REFERENCE`. No production depth or global scale
was selected; K-state, runtime readiness, formal-v8 freezing, and formal
execution were not attempted. `formal_execution_count=0`. This is the terminal
result for the current K-state route unless the user explicitly activates a
new model or route.

Checkpoint A implementation evidence and the exact 96-case
`planned_not_executed` manifest remain preserved under the v6 hash
`0361f609faf56cbc542f07be65abece0b8875aa0f9f8f9ea2539c098d2efdab1`.
No formal-v8 config or scientific dataset exists. Detailed v7/v8 metrics are
routed through the evidence index and task reports rather than repeated here.

## Scope Boundary

Do not extend the depth ladder, re-anchor a comparator, run a formal case,
train a PINN, generate Phase 2 data, run inverse work, fit source parameters,
modify frozen GT, repair M44, execute NbO2, or add nonzero dual-device
coupling. Phase 1 and all downstream positive claims remain `forbidden`.
