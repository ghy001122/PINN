# Next Actions

## Authoritative Current Queue

- Phase/checkpoint: `Q2_PHASE1D_M1_LATENT_SOLVER_PROJECTED_PINN_MVE` /
  `Q2_M1_LATENT_SOLVER_PROJECTED_PINN_MVE_V1_VALID_GO`.
- Active bounded candidate: `M1-LatentProj-PINN` on
  `state-conditioned quasi-static electrothermal fields`; the direct-coordinate
  R1-Lite route is stopped historical evidence.
- Completed: PR #38 is frozen and merged unchanged; the dense differentiable M1
  operator passes 12/12 parity cases; the rank-2 latent model completed one
  1500-step run; N2 passed 1/2 complete test cases with `17.900x` median speedup.
  Disposition is `GO_M1_LATENT_PROJECTION_PINN_MVE`; analytic A2 passed the
  same fast thresholds on 2/2 tests and currently has lower mean field error.
- Preserve `NO_GO_CC_B_STABLE_PATTERNED_TRANSITION_SPAN` and its 34/34
  positive-unstable records as immutable historical/reviewer-defense evidence.

## Single Next Priority

Preregister `Q2_M1_LATENT_PROJECTION_PINN_FORMAL_OOD_V1` with analytic A2 as
the first neural-specific comparator, 36 cases, locked contact/sink OOD, five
seeds, matched budgets, break-even queries, and an M2 model-form holdout/refusal
rule. Do not execute it without a separate task;
inverse, MoE, NbO2, dynamic RC, current-clamp work, and new neural-forward
rescues remain unauthorized.
