# N0 optimizer-forensics and SID/EC-OQ literature red team

This registry records nearest primary precedents and claim boundaries for the prompt-29 ideas. A bounded search can support only a novelty hypothesis; it cannot prove that no prior work exists.

| Project idea | Nearest primary precedent | Abstract / method boundary | Discussion / conclusion boundary | Same and different here | This round's MVE, gate, and stop rule |
|---|---|---|---|---|---|
| Instrumented composite-loss optimizer forensics | Wang, Teng & Perdikaris, *SIAM J. Sci. Comput.* 2021, [doi:10.1137/20M1318043](https://doi.org/10.1137/20M1318043) | Identifies numerical stiffness and unbalanced back-propagated gradients; proposes gradient-statistic annealing and an architecture. | Demonstrated on its benchmark suite; it does not establish that every Adam/L-BFGS failure is caused by gradient imbalance. | Same: per-block gradients and conflict. Different: this round diagnoses one frozen phase-transition CV-PINN and permits only one preregistered recovery. | Exact replay with persistent telemetry. Recover only if post-Adam is finite/scoreable, no metric is over 20x gate, and first nonfinite is isolated to strong Wolfe; otherwise stop N0. |
| Adam/L-BFGS loss-landscape diagnosis | Rathore et al., ICML 2024, [PMLR 235](https://proceedings.mlr.press/v235/rathore24a.html) | Links PINN ill-conditioning to differential operators, compares Adam/L-BFGS, and proposes NysNewton-CG. | Benchmark superiority does not make NNCG a universal cure or identify this repository's first nonfinite tensor. | Same: optimization pathology and second-order context. Different: no new optimizer is introduced; NNCG is prior art and is not run. | Capture the first nonfinite closure/tensor and checkpoint path. One recovery failure permanently stops optimizer arms. |
| Gradient-alignment / SOAP interpretation | Wang et al., 2025, [arXiv:2502.00604](https://arxiv.org/abs/2502.00604) | Develops a multi-gradient alignment score and analyzes SOAP as a Hessian-preconditioning approximation on PINNs. | A broad PINN benchmark does not establish phase-transition-device sensitivity fidelity. | Same: gradient cosine/alignment diagnostics. Different: SOAP is neither the contribution nor an authorized arm. | Record norm ratio and alignment; use only the already preregistered balancing arm or float64 L-BFGS, never SOAP plus another change. |
| Event-aware differentiable dynamics | Chen, Amos & Nickel, ICLR 2021, [OpenReview](https://openreview.net/forum?id=kW_zpEmMLdP) | Differentiates implicitly defined event times and hybrid jumps in Neural Event ODEs. | It does not establish VO2/NbOx observable-quotient rotation or terminal-only identifiability. | Same: event time can alter sensitivities. Different: B uses frozen-solver finite differences and fixed physical event windows, not a learned event function. | Compare fixed pre/switch/post/recovery windows. If h/h2/Richardson differs by over 5%, forbid rank/angle wording. |
| Identifiable combinations / quotient coordinates | Eisenberg & Hayashi, *Math. Biosci.* 2014, [doi:10.1016/j.mbs.2014.08.008](https://doi.org/10.1016/j.mbs.2014.08.008) | Combines Fisher-information subset selection and profile likelihood to recover identifiable parameter combinations. | Demonstrations are model-specific and do not imply that any proposed quotient is physically unique. | Same: Fisher/SVD subspaces and identifiable combinations. Different: event/protocol windows in a frozen phase-transition terminal solver are the candidate empirical distinction. | Noise-whitened terminal Jacobians across three legal protocols and parameter neighborhoods. No stable rotation/rank change means return to fixed rank-1 `gamma_sub`. |
| SID dual geometry (information geometry versus training geometry) | Wang et al. 2021 and Rathore et al. 2024 above | Prior work already studies loss-gradient imbalance and ill-conditioning; Fisher/SVD geometry is also established. | Neither source proves a general link between observation Fisher geometry and residual-training geometry. | Same: two known geometries. Different: the proposed relation is only their event-window co-localization in this frozen device problem. | Solver `F=J_y,p^T Sigma^-1 J_y,p`; checkpoint residual `G=J_r,theta^T J_r,theta` via randomized VJP, never a dense Hessian. Retain SID only with >=2x information, >=10x condition worsening, and stable directions for >=3 protocols/neighborhoods. |
| Quotient-goal DWR-PINN (future, not run) | Roth, Schroeder & Wick 2022, [doi:10.1007/s42452-022-04938-9](https://doi.org/10.1007/s42452-022-04938-9) | Uses neural-network-guided adjoints in dual-weighted-residual goal-oriented error estimation. | The authors note reliance on strong adjoint formulations can limit nonlinear coupled multiphysics extensions. | Same: QoI-weighted residual allocation. Different: a future quotient QoI in the present full PINN would need its own adjoint-consistency proof. | Run only after SID/EC-OQ passes. Compare uniform, residual-adaptive, and QoI-DWR under matched budgets; stop if quotient error/compute is not improved. |
| Conditional inverse/operator with abstention (future, not run) | Lu et al., DeepONet, *Nat. Mach. Intell.* 2021, [doi:10.1038/s42256-021-00302-5](https://doi.org/10.1038/s42256-021-00302-5); Li et al., PINO, [OpenReview](https://openreview.net/forum?id=dtYnHcmQKeM) | DeepONet learns nonlinear operators; PINO combines operator learning with physics-based function optimization. | Operator approximation does not certify inverse identifiability, conservation, OOD safety, or abstention calibration. | Same: conditional protocol-to-state/operator mapping. Different: future output would be only solver-supported quotient/equivalence coordinates with conservation/OOD/rank abstention. | Run only after full-PINN forward and sensitivity-fidelity gates. Stop inverse claims if PINN Jacobian/subspace fails the independent solver. |
| Quotient collapse / leave-one-protocol or geometry (future, not run) | Identifiability-combination work above; operator-learning precedents above | Existing methods already warn that observation design controls identifiable directions and generalization. | Random point splits do not test protocol or geometry extrapolation. | Same: identifiability and operator generalization. Different: group-held-out protocol/geometry is the required split. | Solver-first leave-one-protocol and leave-one-geometry only. Stop the quotient story if scale or direction fails group holdout. |
| H1 branch-memory defect hypothesis (future, not run) | Roussel et al., differentiable Preisach modeling, [arXiv:2202.07747](https://arxiv.org/abs/2202.07747) | Combines classical Preisach hysteresis with differentiable/ML optimization and demonstrates physical magnet hysteresis modeling. | It is not a VO2/NbOx device equation and does not make a Preisach/event head novel. | Same: explicit branch memory. Different: H1 would be tested only as a missing-mechanism correction after an external holdout points to branch error. | Add branch memory alone; require preregistered holdout improvement without damaging conservation. Failure removes H1. |
| H2 latent-heat enthalpy hypothesis (future, not run) | Madir et al., phase-change heat PINN, [arXiv:2410.14216](https://arxiv.org/abs/2410.14216); phase-change enthalpy PINN, [doi:10.1016/j.cpc.2025.109854](https://doi.org/10.1016/j.cpc.2025.109854) | Existing PINNs already encode phase interfaces/enthalpy and report interface-region difficulty. | Those thermal phase-change settings do not prove latent heat is required in this oxide device. | Same: enthalpy/latent heat is an established component. Different: H2 is a single-mechanism defect test against a pre-existing holdout residual signature. | Add enthalpy alone only if holdout energy residual localizes at transition. Stop if energy/trajectory prediction does not improve under the locked gate. |
| H3 field-dependent conductivity plus implicit current root (future, not run) | Existing electrothermal compact/device solvers and implicit circuit solves are prior numerical components; no finite search can establish exclusivity. | Field-dependent transport and implicit circuit closure are established modeling ideas. | A better fit alone cannot distinguish field dependence from memory or latent heat. | Same: nonlinear conductivity/current closure. Different: H3 would be isolated only after H1/H2 are separately rejected by holdout signatures. | Add H3 alone with a differentiable implicit-root residual and independent-solver parity. Stop on non-unique roots, derivative failure, or no holdout improvement. |

## Component non-novelty lock

NNCG, SOAP, PirateNet/residual-adaptive architectures ([arXiv:2402.00326](https://arxiv.org/abs/2402.00326)), causal/time-slab training, IRK-PINN, DWR, PINO, DeepONet, and Preisach/Prandtl-Ishlinskii constructions are established components or baselines. None is a standalone novelty claim in this project.

The previously bounded novelty hypothesis—solver-verified event/protocol observable quotients plus an independently faithful full-PINN sensitivity geometry—fails this round's prerequisite discovery gates. SID and EC-OQ are therefore removed from the active route. The literature search remains a non-exhaustive overlap audit and cannot establish world-first novelty for any future reformulation.
