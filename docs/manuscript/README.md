# Manuscript Versions

`main_submission_v1.md` and `main_submission_v2.md` are protected historical snapshots of the former 1D constrained-inverse route. Their reproducibility and reviewer-defense value remains, but they are logically archived and are not the selected current manuscript.

Do not overwrite v1 or v2. A new evidence-selected v3 is created only after Phase 1 passes and R1 produces a positive, gate-complete result. Until then, research planning belongs in the canonical execution guide and current phase contract, not in result language inside a manuscript.

The degradable paper identity is:

1. R1 `HysGeo-Hybrid-PINN`: minimum route;
2. R2 `GeoPhase-HomoMoE-PINN`: preferred method upgrade if matched-budget synergy passes;
3. R3 observable-subspace/OQ: conditional extension only if solver/PINN sensitivity gates pass.

Current submission status is in `submission_go_no_go.md`. Scientific claim authority remains `docs/paper/final_claim_matrix.md`; historical manuscript paths and lifecycle are indexed by `docs/archive/historical_manuscripts/README.md`.
