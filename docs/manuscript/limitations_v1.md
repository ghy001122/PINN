# Limitations V1

1. All results are synthetic numerical digital-twin evidence; no measured experimental validation has been completed.
2. The main model is one-dimensional and reduced-order. It is not full FEM/3D/device-grade multiphysics.
3. Sparse terminal data do not support unique arbitrary complete hidden-field recovery in the configured benchmark; the audit is not a universal impossibility proof.
4. \(\gamma_{\mathrm{sub}}\) is a lumped effective parameter, not an unconditional material constant.
5. Recovery is conditional on fixed microphysics and fixed or tightly bounded nuisance priors, especially \(T_{\mathrm{sw}}\).
6. The approximately `0.1 K` \(T_{\mathrm{sw}}\) marker is a synthetic threshold under the configured error criterion, not a real-device specification.
7. Noise, seed, observation-count, and off-grid robustness is conditional; wide \(T_{\mathrm{sw}}\) mismatch remains a reproducible failure region.
8. Protocol search is finite and benchmark-specific. The locked 720-case Figure 5 result varies waveform/duration and calibration-error factor together, so it supports only bundled configuration performance, not isolated protocol gain, experimental validity, or global optimality. The broader 2400-case stress audit is supplementary negative evidence.
9. Response-surface sweeps reuse declared simulator anchors; dense surface points are not independent fresh ODE solves.
10. Public VO2 provenance and source semantics are now locked, but D0a fails its time-step convergence gate. No repository calibration or 13 V cross-voltage evaluation was run; this is not external or experimental validation.
11. V10 P1 is field-anchored and fails its strict interface gate; P2 is local linearized recovery and proceeds with rank-deficient selected protocols.
12. Segmented-terminal rank gain applies only to a three-parameter conductivity-profile basis and does not establish full 2D field recovery.
13. OASIS generalization is a ridge preflight, not trained neural-operator evidence; cross-material transfer fails strongly.
14. Full STL/Seiler reproduction, universal Fourier/F-SPS superiority, terminal-only 2D inverse solved, and experimental validation remain `forbidden`.
15. The versioned complete 1D PINN implementation passes its contract/manufactured audit but fails the fixed single-seed forward MVE. Reliability, sensitivity fidelity and inverse recovery remain `forbidden`.
16. The public-data protocol-conditioned quotient hypothesis remains untested because D0a and N0 failed upstream gates. A separate solver-first synthetic SID/EC-OQ discovery audit was run, but its derivative, event-window, stability, and dual-geometry gates fail; it cannot be promoted to public-data or physical protocol-rotation evidence.
17. The N0-R teacher/FVM audit passes and the exact-trace interface implementation improves local interface metrics, but the fixed data-free repair still fails global current/energy, defect/thermal residual, field, and port gates. No sparse-port anchor, seed expansion, SC-LOS, or sensitivity/inverse stage was authorized.
18. Historical N0-CV-E v3 passes all 18 locked no-training checks but remains `runtime_abort_unassessed`, with no scoreable trajectory. The instrumented v3r replay produces a scoreable Adam-1200 trajectory whose port gate passes while residual, field, interface-flux, and ledger gates fail; five metrics exceed `20x` their thresholds. A same-checkpoint diagnostic reproduces a strong-Wolfe non-finite parameter at closure 3. Recovery is ineligible and this optimizer route is permanently stopped; the complete PINN scaffold is retained, but none of this can be cited as trained full-PINN accuracy, conservation, sensitivity, or inverse evidence.
19. Solver-first SID/EC-OQ uses terminal-current geometry only and stays within its forward budget, but derivative agreement passes only `3/9`, one preregistered switch window degenerates to the full trace, and cross-protocol/neighborhood training geometry is absent. SID dual geometry and EC-OQ rotation/rank claims are forbidden.
20. CPCF has no scientific vote. A superseding audit shows that two response-surface protocols are not waveform-equivalent to their fresh-anchor simulators; the fresh solver uses different grid/tolerances, a five-point rather than fifteen-point candidate grid, and a heat term absent from the historical objective. The `T_sw_prior_width_K` label is dimensionally wrong, four noise levels are not iid seed replicates, the bootstrap mixes those levels, all 48 scientific votes use proxy errors, and unqualified points enter Pareto stability. Historical files remain unchanged as non-voting software diagnostics only.
21. CEBA corrects those contracts with exact direct-solver triangle/LTP-LTD trajectories and passes all six historical parity anchors. Its preregistered pilot nevertheless abstains at zero `T_sw` error under every tested observation/noise condition, so no success/failure bracket, `delta_T_sw_star`, or solver-refinement claim is available. The abstention rule was not relaxed after inspection. It also reads the known synthetic `true_gamma`, hard-codes the `0.15` class radius, and depends on candidate-grid span; it is an oracle diagnostic, not a deployable refusal rule.
22. M32 SCIS removes truth access from inference and passes nominal discrete-grid coverage, but accepts all severe `2 K` switching-temperature mismatches while point success is zero. It therefore does not support an operational coverage/refusal certificate, model-mismatch robustness, continuous-parameter coverage, or any experimental/cross-device/cross-material generalization.
23. H1 latent heat, H2 history state, and H3 two-dimensional geometry remain conditional hypotheses, not implemented evidence. They require external discrepancy triggers plus conservation, units, primary literature, and independent-solver gates.
