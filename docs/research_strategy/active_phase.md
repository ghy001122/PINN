# Active Phase

Active phase ID: `M_GAMMA_SUB_MANUSCRIPT_ASSEMBLY`

## Current Phase

`Q2 SCI delivery — calibrated gamma_sub manuscript assembly`

The final bounded N0-CV-E v3 attempt is closed as `failed_but_informative`. Frozen GT v1.1, P0-P4, D0a, earlier N0 failures, and all negative artifacts remain unchanged facts. A complete PINN remains an indispensable paper scaffold, but no positive trained-forward claim is available.

The calibration-gated constrained `gamma_sub` rank-1 result is the safe `qualified_supported` inverse mainline.

## Closed N0-CV-E v3 Evidence

- Phase A documents the local untracked v2 checkpoint, absent remote CI run for base commit `e3f5765`, missing final pytest record, and seven fail-closed v2 gate-coverage gaps without rewriting v2 hashes.
- The older conservation artifact is downgraded to algebraic bookkeeping smoke evidence. Independent adjacent-state storage/flux/source ledgers pass on frozen GT: defect `4.03948e-06`, energy `0.00258234`; a Radau interval replay has maximum relative RMS `1.30271e-07`; tampered trajectories fail.
- The fixed global baseline and v2 split repair were rescored with one SI/frozen registry and one analytic electric head. Neither passes the CV or ledger gates.
- N0-CV-E v3 predicts bounded cell-centered `c_v,T,m`; `phi,sigma,E,J,I,G` follow the frozen analytic series-electric relation. Torch/NumPy electrostatics and Torch/frozen-RHS parity errors are `2.30787e-08` and `2.12586e-08`.
- All 18 locked no-training checks pass, including hashes, hard IC/BC, roundtrip, gradients, current spread, frozen ledgers, and negative-control separation.
- The authorized seed `20260715` run reaches its sole L-BFGS stage after 1200 Adam steps, then stops on a non-finite strong-Wolfe closure. No checkpoint or score trajectory exists; all result gates are `unassessed_fail_closed`.
- No balancing arm, seed expansion, sparse anchor, hyperparameter search, N1-N3, or SC-LOS was run.

Primary report: `docs/codex_reports/n0_cv_e_v3_report.md`.

Machine summary: `outputs/tables/n0_cv_e_v3_summary.json`.

## Single Active Bottleneck

Assemble the final manuscript/submission package from the locked calibrated `gamma_sub` evidence. Include D0a and N0 as explicit limitations and reviewer-defense evidence. Resolve wording and artifact consistency only; do not reopen a scientific training branch.

## Stop And Claim Boundary

N0 tuning is stopped. The operator preflight supports implementation consistency, not learned forward accuracy. Reliable full-PINN forward evidence, sensitivity fidelity, quotient inverse recovery, protocol-dependent identifiability, independent external validation, and experimental validation remain `forbidden`.
