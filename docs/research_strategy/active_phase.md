# Active Phase

Active phase ID: `Q2_POST_M44_SUBMISSION_FREEZE_AND_UPLOAD_LOCK`

## Objective and manuscript use

Freeze the scientific result set after the preregistered M44 stop decision,
finish evidence integrity and release validation, then close journal selection,
declarations, rendering, visual QA, and lawful archive routing for submission.
No new scientific experiment is authorized in this phase.

The manuscript remains an identifiability-gated, synthetic numerical
physics-informed digital-twin inverse study. Its positive line is the tightly
`T_sw`-calibrated rank-1 constrained `gamma_sub` result. M43 contributes a manufactured
finite-width thermal-component verification; M44 contributes a negative
heterogeneous-geometry convergence and model-validity boundary. Neither is a
Qiu device, experimental, inverse, or PINN validation.

## M44 terminal decision

M44 executed exactly 31 preregistered thermal-only forwards in one receipt-
bearing run. Independent homogeneous and layered limits, source integration,
power/energy conservation, domain closure, local `Tmax`, x-z bias convergence,
provenance, and budgets passed. Three mandatory gates failed:

- `zth_mesh_pair_change = 0.0632464 > 0.02`;
- `zth_time_pair_change = 0.0544910 > 0.02`;
- `vo2_mean_temperature_pair_change = 0.0632464 > 0.02`.

Final status is `failed_but_informative`; decision is
`M44_STOP_REAL_GEOMETRY_UPGRADE`. Domain change was only `2.05e-7`, so the
failure is not a remote-boundary truncation effect. The three registered source
families differ by `0.0335790` of M43 `Rref`; this threshold classification is
descriptive and non-voting because the registered discretization differences
are larger. Their non-voting departure from the homogeneous M43 response
reaches `3.24964`. Pure x-z quantitative use is forbidden from
`1.285714e-8` through `5.22e-6` s for every source family on the locked time
grid; M44 did not independently close the x-z time-step error.

## Locked routing

- No M44 repair round or budget expansion.
- M45 one-RC/two-RC/causal-kernel reduction is not authorized.
- Qiu/Zhang reproduction rescue is closed absent genuinely new author code or
  raw data.
- Frozen GT v1.1, M40/M40R, M42/M43, E1F/E1F-R, and the calibrated
  `gamma_sub` evidence remain byte protected.
- M42's `1.330233` source/local resistance mismatch and unresolved latent heat
  remain upstream blockers.

## Allowed work

- result-lock tests, governance audit, one complete test suite, and release
  evidence for the committed M44 state;
- manuscript/SI limitation integration without upgrading a scientific claim;
- official journal/article-type verification, author declarations, template
  rendering, visual QA, and public/restricted asset instructions;
- submission preparation and reviewer-defense refinement using locked evidence.

## Forbidden

New thermal PDE runs, M44R/M45, Qiu fitting, local Joule-map inference, device
coupling, latent heat, hysteresis, inverse identification, PINN training,
Zhang 13 V, M40/M40R/M43 reruns, threshold relaxation, device-grade 3D/FEM,
external or experimental validation wording, and any attempt to use new
science to conceal a submission-package defect.
