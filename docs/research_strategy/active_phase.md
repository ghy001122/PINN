# Active Phase

Active phase ID: `Q2_M44_QIU_HETEROGENEOUS_3D_THERMAL_BRIDGE_AND_REPRODUCTION_CLOSEOUT`

## Objective and manuscript use

Extend the locked M43 manufactured half-space component into one bounded,
source-provenance-graded heterogeneous 3D sensible-heat family at Qiu-reported
length scales. The round must independently recover homogeneous and layered
limits, quantify source-location nuisance and matched x-z reduction error, and
permanently close the current Qiu/Zhang reproduction-rescue responsibility.

A pass may support only a qualified literature-scale-constrained thermal
operator family. A failure closes the real-geometry upgrade. Neither outcome
validates Qiu dynamics, a real device, inverse identification, or a PINN. The
constrained `gamma_sub` mainline remains frozen and unchanged. The previously
planned one-RC/two-RC/causal-kernel comparison is superseded as M44 and is
conditionally deferred to M45.

## Upstream decisions

M42 remains `failed_but_informative`, decision B:

- source/local resistance error `1.33023`;
- domain sensitivity `0.84235`;
- mesh fine-pair sensitivity `0.13813`;
- time fine-pair sensitivity `0.00886`;
- finite-width/x-z closure error `0.67058`;
- smooth discrete enthalpy ledger `2.58e-14`;
- switching enthalpy unassessed because latent heat is not source-locked.

M43 is `qualified_supported`, decision
`M43_THERMAL_CLOSURE_GO_COMPONENT_ONLY`: `15` unique thermal PDE forwards and
all `21/21` preregistered gates passed for a manufactured finite rectangular
isoflux source on a homogeneous isotropic half-space. This does not supersede
any M42 failure or provide device/external evidence.

## Allowed M44 scope

- additive M43 post-commit attestation and classic-reproduction closeout;
- constant-property, small-signal, sensible-heat-only VO2/Ti/Au/Al2O3 family;
- a conservative heterogeneous 3D thermal FVM and matched x-z comparator;
- independent homogeneous-half-space and layered-one-dimensional limits;
- equal-power bulk, end-localized engineering-prior, and mixed source families;
- mesh/domain/time convergence, power/energy ledgers, provenance, source
  envelope, and a fail-closed terminal decision.

Before any thermal forward, lock the case matrix, source family, material and
geometry responsibility, budget, thresholds, decision logic, and exact
allowed/forbidden wording. No post-result gate or nuisance-family change is
permitted.

## Stop rule

M44 is one bounded bridge, not a parameter search or device rescue. A failed
independent reference, provenance, conservation, or convergence gate produces
`M44_STOP_REAL_GEOMETRY_UPGRADE`. Passing numerics with a large source envelope
produces an explicit conditional/abstention boundary. No M44 repair round or
budget expansion is authorized.

## Forbidden

Qiu parameter fitting, local electrical-potential/Joule-map inference, device
coupling, author-code equivalence, M40/M40R/M43 rerun, M41, local use of lumped
`Cth/Sth`, unsourced latent heat, active hysteretic device forward, RC/kernel
fitting, `gamma_eff` or new `gamma_sub` claim, inverse or PINN, Zhang 13 V,
external-validation, experimental, arbitrary-field, or successful-neural
claims.
