# Active Phase

Active phase ID: `M_GAMMA_SUB_MANUSCRIPT_ASSEMBLY`

## Current phase

`Q2 SCI delivery — calibrated gamma_sub manuscript assembly`

The user-authorized `N0_OPTIMIZER_FORENSICS_AND_SID_DISCOVERY` phase is closed. It does not replace frozen GT v1.1, P0-P4, D0a, historical N0 failures, or the existing evidence lock.

The constrained `gamma_sub` result, calibration-gated and rank-1, remains the safe `qualified_supported` inverse mainline. A complete PINN remains the mandatory paper scaffold, but no trained full-PINN forward, sensitivity, or inverse claim is supported.

## Closed prompt-29 evidence

### A. N0-CV-E v3r optimizer forensics

- Historical commit `b3800fd` remains `failed_but_informative / runtime_abort_unassessed`; its missing score trajectory is not scientific model falsification.
- The locked replay at commit `18b703a` completes 1200 Adam steps and yields a scoreable post-Adam trajectory.
- Port NRMSE95 passes narrowly (`0.0955475 <= 0.10`), but residual, field, interface-flux, energy-ledger, and defect-ledger gates fail. Five metrics exceed their locked gates by at least `20x`.
- A serializer-safe diagnostic replay from the same pre-L-BFGS checkpoint reproduces the non-finite strong-Wolfe failure at closure 3; the first non-finite parameter is `backbone.net.0.weight`.
- The recovery prerequisite is false. Neither float64 nor gradient-statistics balancing nor seed expansion was run. This N0 optimizer route is permanently stopped; the complete multidomain PINN architecture remains the required scaffold.

### B. Solver-first SID/EC-OQ discovery

- Only `3/9` derivative cases meet the `<=5%` agreement gate; maximum discrepancy is `0.648429`.
- The preregistered low-amplitude triangle switch rule degenerates to the full trace and was not moved post hoc.
- Corrected angle, training-condition, protocol-count, and neighborhood-stability gates fail. Isolated rank/information diagnostics cannot vote after derivative failure.
- The preregistered SID/EC-OQ implementation is rejected and inactive. Its broad scientific hypothesis is not declared permanently false; any revisit requires a new user-authorized numerical derivative/event-window contract.

### C. Bounded CPCF pilot

- `48` cases, `12` operating points, `8` fresh solver anchors, and `48` solver forward evaluations were run under the locked evidence thresholds.
- Gate a fails: mean anchor discrepancy `0.299600 > 0.0537361`, classification agreement `0.625 < 0.883333`, and `3 > 0` disagreements.
- Gate b passes with `5` stable sample-nondominated points. Gates c and d fail because no locked `20%` cost/risk improvement exists and no improvement-direction CI can exclude zero.
- CPCF is `failed_but_informative`; the full sweep is stopped. The decision table and plot are supplementary only and support no cost-optimality claim.

Primary report: `docs/codex_reports/prompt30_a7c_audit_and_cpcf_report.md`.

## Single active bottleneck

Assemble the final manuscript, submission package, and reviewer-defense package from the locked calibrated `gamma_sub` evidence. Include D0a, N0 v1/v2/v3/v3r, current SID/EC-OQ, and CPCF as explicit failure boundaries. Do not reopen a scientific training or identifiability branch.

## Claim boundary

Reliable full-PINN forward fidelity, PINN sensitivity fidelity, quotient inverse recovery, SID dual geometry, protocol-dependent EC-OQ rank/rotation, independent external validation, experimental validation, full hidden-field recovery, full STL reproduction, and universal Fourier/F-SPS superiority remain `forbidden`.
